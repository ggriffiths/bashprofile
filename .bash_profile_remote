# Variables
export GOPATH=~/workspace/go
export GOWS=$GOPATH/src/github.com
export GOWSK=$GOPATH/src/github.com/kubernetes-csi
export WSKU=~/workspace/kubeup
export WST=~/workspace/test
export PATH=$PATH:$GOPATH/bin
export PATH=$PATH:~/bin
export PATH=$PATH:~/apps/
export GITHUB_USER=ggriffiths
export FORK_REMOTE=github
export UPSTREAM_REMOTE=origin
export KUBECONFIG=~/workspace/kubeup/kubeconfig-k8s-1.conf
export DOCKER_HUB_REPO=docker.io/ggriffiths
export KUBEUP_USER=grant

# Aliases 
alias ebp="vi ~/.bashrc"
alias sbp="source ~/.bashrc"
alias gows="cd $GOWS"
alias ku="cd $WSKU"
alias wt="cd $WST"
alias gowsk="cd $GOWSK"
alias l="ls -ltra"
alias k='kubectl'
alias kn='kubectl -n kube-system'
alias cluster1='export KUBECONFIG=~/workspace/kubeup/kubeconfig-k8s-1.conf'
alias cluster2='export KUBECONFIG=~/workspace/kubeup/kubeconfig-k8s-2.conf'

# Settings
export HISTTIMEFORMAT="%m/%d/%y %T "

